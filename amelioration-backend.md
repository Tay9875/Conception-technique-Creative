# Amelioration Backend

- Migration backend vers TypeScript.
- Auth JWT access + refresh + rotation/revocation.
- Validation Zod + format d'erreur unifie.
- RBAC moderation.
- Rate limit + helmet + compression.
- CORS durci (strict prod).
- Metrics Prometheus (`/metrics`).
- Migrations versionnees Knex + rollback.
- Scripts DB (`migrate`, `seed`, `rollback`, `purge-tokens`).
- Tests unitaires + integration API.
- CI backend renforcee + CodeQL.
