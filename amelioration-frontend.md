# Amelioration Frontend

- Alignement complet avec le nouveau contrat API backend (`success/data/error`).
- Ajout d'un client API central (`apiClient`) pour normaliser les erreurs.
- Auth adaptee (stockage token + refreshToken + user).
- Endpoints alignes (`/users/me`, `/comments/:postId`, likes/report avec Bearer).
- Ecrans principaux adaptes: Auth, Accueil, Article, Feed, MesArticles, Profile, BlogCard, CommentSection.
